# ⚡ Быстрая настройка Secrets для Lavsit Textile

## 🎯 Автоматическая настройка (без ручного ввода)

Большинство переменных уже известны из проекта! Скрипт использует их автоматически.

## 🚀 Быстрый старт

### 1. Установите необходимые инструменты (один раз)

```powershell
# GitHub CLI
winget install GitHub.cli

# Vercel CLI (если еще не установлен)
npm install -g vercel
```

### 2. Авторизуйтесь (один раз)

```powershell
# GitHub
gh auth login

# Vercel  
vercel login
```

### 3. Запустите автоматическую настройку

```powershell
# Перейдите в директорию проекта
cd "E:\Work programs\cursor\repositary\lavsit-textile"

# Запустите скрипт
npm run secrets:setup
```

## ✅ Что будет настроено автоматически

Скрипт автоматически использует данные, которые уже есть в проекте:

- ✅ **DATABASE_URL** - построится из известных Supabase credentials
- ✅ **VERCEL_TOKEN** - из файла `.vercel-token` или известных данных
- ✅ **VERCEL_ORG_ID** - из известных данных проекта
- ✅ **VERCEL_PROJECT_ID** - из известных данных проекта

## 📋 Что может потребоваться

Если скрипт обнаружит недостающие переменные, он покажет инструкции. Обычно нужно:

### Если нужно добавить EMAIL_CHECKER_API_KEY (опционально)

Создайте файл `.env.local`:
```env
EMAIL_CHECKER_API_KEY="your-secret-key-here"
```

Затем запустите скрипт снова:
```powershell
npm run secrets:setup
```

## 🔍 Проверка результата

### Проверить GitHub Secrets:
```powershell
gh secret list
```

### Проверить Vercel Environment Variables:
```powershell
vercel env ls
```

## 💡 Альтернативные команды

```powershell
# Только GitHub Secrets
npm run secrets:github-only

# Только Vercel Environment Variables
npm run secrets:vercel-only

# Dry Run (проверка без изменений)
npm run secrets:setup-dry
```

## ⚠️ Если что-то пошло не так

1. **Убедитесь, что авторизованы:**
   ```powershell
   gh auth status
   vercel whoami
   ```

2. **Проверьте, что проект связан с Vercel:**
   ```powershell
   vercel link
   ```

3. **Проверьте наличие файла .env.local:**
   ```powershell
   # Если нужно создать
   echo DATABASE_URL="postgresql://..." > .env.local
   ```

## 📚 Подробная документация

См. [AUTOMATION_SECRETS_PLAN.md](./AUTOMATION_SECRETS_PLAN.md) для полной информации.

---

**Готово!** Запустите `npm run secrets:setup` и всё настроится автоматически! 🎉


