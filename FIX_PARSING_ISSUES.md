# Исправление проблем с парсингом

## Дата: 2026-01-28

### Проблемы и решения

#### 1. Артекс - "Не удалось скачать файл за последние 30 дней"

**Проблема:** URL в базе данных был старый формат `25.12.2025-2.xlsx`

**Решение:**
- ✅ Обновлен URL в базе данных на новый формат: `https://artextkani.ru/wp-content/uploads/YYYY/MM/DD.MM.YYYY-1.xlsx`
- ✅ Улучшена логика парсера для правильной обработки нового формата с папками `/2026/01/`
- ✅ Парсер теперь правильно заменяет плейсхолдеры: `YYYY`, `MM`, `DD.MM.YYYY`

**Выполнено:**
```bash
npm run db:fix-arteks
```

**Результат:** URL обновлен в базе данных, парсер готов к работе с новым форматом.

#### 2. Аметист - "No email attachments found"

**Проблема:** Автоматическая проверка email не находит письма

**Решение:**
- ✅ Улучшена логика автоматической проверки email в `/api/suppliers/[id]/parse`
- ✅ Добавлен fallback: если с фильтрами не находит, пробует без фильтров и фильтрует вручную
- ✅ Улучшена логика поиска в `/api/suppliers/[id]/parse-email` - также пробует без фильтров

**Что нужно проверить:**
1. Email конфигурация настроена: `POST /api/suppliers/{id}/email-config`
2. Правильные фильтры:
   - From: `noreply@ametist.ru`
   - Subject: `Остатки номенклатуры на складе 'ООО Аметист'`
3. Email аккаунт имеет доступ к входящим письмам

**Проверка конфигурации:**
```bash
npm run check:email-configs
```

#### 3. Нортекс - "No email attachments found"

**Проблема:** Автоматическая проверка email не находит письма

**Решение:**
- ✅ Улучшена логика автоматической проверки email в `/api/suppliers/[id]/parse`
- ✅ Добавлен fallback: если с фильтрами не находит, пробует без фильтров и фильтрует вручную
- ✅ Улучшена логика поиска в `/api/suppliers/[id]/parse-email` - также пробует без фильтров

**Что нужно проверить:**
1. Email конфигурация настроена: `POST /api/suppliers/{id}/email-config`
2. Правильные фильтры:
   - From: `obivkanortex@mail.ru`
   - Subject: не требуется (можно оставить пустым)
3. Email аккаунт имеет доступ к входящим письмам

**Проверка конфигурации:**
```bash
npm run check:email-configs
```

## Улучшения логики поиска email

### Автоматический fallback

Если письма не найдены с фильтрами (fromEmail, subjectFilter), система автоматически:
1. Пробует поиск без фильтров (только по дате)
2. Фильтрует найденные письма вручную по fromEmail и subjectFilter
3. Это позволяет найти письма даже если IMAP фильтры работают некорректно

### Логика работает в двух местах:
1. `/api/suppliers/[id]/parse` - автоматическая проверка email при парсинге
2. `/api/suppliers/[id]/parse-email` - ручная проверка email

## Следующие шаги

### Для Аметиста и Нортекса:

1. **Проверьте email конфигурацию:**
   ```bash
   npm run check:email-configs
   ```

2. **Если конфигурация отсутствует, настройте через API:**
   ```bash
   POST /api/suppliers/{supplierId}/email-config
   ```

3. **Попробуйте запустить парсинг снова:**
   - Система автоматически попытается найти письма
   - Если с фильтрами не найдет, попробует без фильтров

### Для Артекса:

1. ✅ URL обновлен в базе данных
2. ✅ Парсер обновлен для нового формата
3. **Попробуйте запустить парсинг снова** - должно работать

## Проверка работы

После исправлений проверьте:

1. **Артекс:**
   - Парсинг должен найти файл по новому URL формату
   - Файл должен скачаться и распарситься

2. **Аметист:**
   - Автоматическая проверка email должна найти письма
   - ZIP архив должен распаковаться
   - Excel файл должен распарситься

3. **Нортекс:**
   - Автоматическая проверка email должна найти письма
   - Excel файл должен распарситься

## Если проблемы сохраняются

1. Проверьте логи в консоли - там будет детальная информация о поиске email
2. Убедитесь, что email конфигурация правильная
3. Проверьте, что письма действительно есть в почтовом ящике
4. Попробуйте запустить `/api/suppliers/{id}/parse-email` вручную для диагностики

