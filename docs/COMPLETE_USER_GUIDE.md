# Полная инструкция пользователя Lavsit Textile

> **Версия:** 2.0  
> **Дата обновления:** 2025-01-28

---

## Содержание

1. [Введение](#введение)
2. [Начало работы](#начало-работы)
3. [Работа с тканями](#работа-с-тканями)
4. [Работа с поставщиками](#работа-с-поставщиками)
5. [Настройка парсинга](#настройка-парсинга)
6. [Email интеграция](#email-интеграция)
7. [Фильтры и поиск](#фильтры-и-поиск)
8. [Редактирование данных](#редактирование-данных)
9. [Категории тканей](#категории-тканей)
10. [Исключения из парсинга](#исключения-из-парсинга)
11. [Ручная загрузка данных](#ручная-загрузка-данных)
12. [Экспорт данных](#экспорт-данных)
13. [Правила парсинга для каждого поставщика](#правила-парсинга-для-каждого-поставщика)
14. [Решение проблем](#решение-проблем)

---

## Введение

**Lavsit Textile** — это система для автоматического парсинга, хранения и управления данными о тканях от различных поставщиков. Система позволяет:

- Автоматически получать данные о наличии тканей от 14 поставщиков
- Парсить прайс-листы для установки цен
- Управлять категориями тканей по цене
- Фильтровать и сортировать ткани по различным параметрам
- Редактировать данные вручную
- Исключать ткани из автоматического парсинга
- Экспортировать данные в Excel

### Основные разделы

- **Ткани** (`/fabrics`) — просмотр и управление всеми тканями
- **Поставщики** (`/suppliers`) — управление поставщиками и парсингом
- **Категории** (`/categories`) — управление категориями тканей
- **Исключения** (`/exclusions`) — управление исключениями из парсинга
- **Палитра** (`/palette`) — визуальная палитра цветов тканей

---

## Начало работы

### Первый запуск

1. Откройте приложение в браузере (по умолчанию http://localhost:4001)
2. Перейдите на страницу "Поставщики"
3. Для каждого поставщика настройте метод парсинга:
   - **HTML парсинг** — укажите URL страницы с данными
   - **Excel по URL** — укажите URL Excel файла
   - **Email парсинг** — настройте подключение к почтовому серверу

### Настройка поставщика

1. Нажмите кнопку ⚙️ (Настройки) рядом с поставщиком
2. Выберите метод парсинга
3. Заполните необходимые поля:
   - Для HTML/Excel: URL для парсинга
   - Для Email: настройки IMAP (см. раздел [Email интеграция](#email-интеграция))
4. Нажмите "Сохранить"

### Первый парсинг

1. Нажмите кнопку 🔄 (Обновить) рядом с поставщиком
2. Если это первый парсинг, система автоматически проведет анализ структуры данных
3. Ответьте на вопросы в диалоговом окне для настройки правил парсинга
4. После сохранения правил данные будут автоматически распарсены
5. Просмотрите результаты на странице "Ткани"

---

## Работа с тканями

### Просмотр тканей

Страница "Ткани" (`/fabrics`) отображает все ткани в виде таблицы с группировкой по коллекциям.

#### Структура таблицы

**Колонки:**
- **Поставщик** — название поставщика ткани
- **Коллекция** — название коллекции (кликабельно для редактирования)
- **Цвет** — номер/название цвета
- **Наличие** — статус наличия (в наличии / нет в наличии / неизвестно)
- **Метраж** — количество метров в наличии
- **Цена** — цена ткани (кликабельно для редактирования)
- **Категория** — категория ткани (1-16, определяется автоматически по цене)
- **Тип ткани** — тип ткани (кликабельно для редактирования)
- **Описание** — описание ткани (кликабельно для редактирования, раскрывается по клику)
- **Изображение** — кнопка для загрузки изображения ткани
- **Дата обновления** — дата последнего обновления данных
- **Дата поступления** — дата следующего поступления (если указана)
- **Комментарий** — дополнительные комментарии

#### Группировка по коллекциям

Ткани автоматически группируются по коллекциям. Для каждой коллекции отображается:
- Заголовок с названием коллекции
- Кнопка "Свернуть/Развернуть категорию" для сворачивания/разворачивания коллекции
- Список всех цветов в коллекции

#### Счетчики

В верхней части страницы отображаются динамические счетчики:
- **Общее количество тканей** — количество тканей с учетом активных фильтров
- **Общее количество коллекций** — количество коллекций с учетом фильтров
- **Общее количество поставщиков** — количество поставщиков с учетом фильтров

---

## Работа с поставщиками

### Страница поставщиков

Страница "Поставщики" (`/suppliers`) отображает таблицу всех поставщиков с информацией:
- Название поставщика
- Метод парсинга (HTML, Excel, Email)
- URL/Email — ссылка на источник данных
- Количество тканей
- Статус (активен / ошибка)
- Дата последнего обновления

### Действия с поставщиком

#### Кнопки действий

- **🔗 (Внешняя ссылка)** — открывает сайт поставщика в новой вкладке
- **🔄 (Обновить)** — запускает парсинг данных поставщика
- **📊 (Загрузить наличие)** — ручная загрузка файла с наличием (Excel)
- **💰 (Загрузить прайс-лист)** — ручная загрузка прайс-листа (Excel)
- **🗑️ (Очистить все данные)** — удаление всех тканей поставщика
- **⚙️ (Настройки)** — настройка параметров парсинга

#### Обновление данных

**Автоматическое обновление:**
1. Нажмите кнопку 🔄 рядом с поставщиком
2. Система автоматически:
   - Для email-поставщиков: проверяет новые письма (если файлы отсутствуют на диске)
   - Загружает данные из источника
   - Парсит данные согласно правилам
   - Обновляет ткани в базе данных

**Обновление всех поставщиков:**
1. Нажмите кнопку "Обновить все" в верхней части страницы
2. Система запустит парсинг для всех поставщиков последовательно

#### Очистка данных

**Удаление всех тканей поставщика:**
1. Нажмите кнопку 🗑️ рядом с поставщиком
2. Подтвердите удаление в диалоговом окне
3. Все ткани поставщика будут удалены из базы данных
4. Счетчик тканей обновится до 0

**Внимание:** Это действие нельзя отменить!

---

## Настройка парсинга

### Настройки поставщика

#### HTML парсинг

**Настройки:**
- **URL для парсинга** — URL страницы с данными о тканях

**Используется для:**
- Artvision
- Viptextil
- TextileData
- TextileNova

#### Excel по URL

**Настройки:**
- **URL для парсинга** — URL Excel файла

**Используется для:**
- NoFrames
- Vektor (динамический URL)
- Эгида (динамический URL)
- Artefact
- Союз-М
- Домиарт
- Артекс
- Tex.Group

**Особенности:**
- Для Vektor и Эгида URL динамический (содержит дату)
- Система автоматически ищет актуальный файл по датам

#### Email парсинг

**Настройки:**
- **IMAP Host** — адрес IMAP сервера (например, imap.gmail.com)
- **IMAP Port** — порт IMAP сервера (обычно 993)
- **Email (User)** — адрес электронной почты
- **App Password** — пароль приложения (для Gmail/Mail.ru)
- **Secure (TLS)** — использовать TLS (обычно Да)
- **Отправитель (From Email)** — фильтр по отправителю (опционально)
- **Фильтр по теме письма** — фильтр по теме (опционально)
- **Период поиска (дней)** — период поиска писем (по умолчанию 90)
- **Проверять в том числе прочитанные письма** — если включено, ищутся все письма; если выключено, только непрочитанные
- **Использовать любое последнее вложение** — если включено, используется последнее вложение (включая обработанные); если выключено, только необработанные

**Используется для:**
- Аметист
- Нортекс

**Особенности:**
- Для Gmail и Mail.ru требуется App Password (при включенной 2FA)
- Система автоматически выбирает самое позднее письмо с валидным Excel-вложением
- При парсинге автоматически запускается "Check Email", если файлы отсутствуют на диске

### Правила парсинга

Правила парсинга настраиваются автоматически при первом парсинге через диалог вопросов. Правила включают:

- **Индексы столбцов** — какие столбцы содержат коллекцию, цвет, наличие, метраж, цену, дату, комментарий
- **Строки для пропуска** — какие строки нужно пропустить (заголовки, служебные строки)
- **Строка заголовка** — номер строки с заголовками
- **Паттерны для пропуска** — текстовые паттерны для пропуска строк
- **Специальные правила** — специфичные правила для каждого поставщика

---

## Email интеграция

### Настройка подключения

#### Gmail

1. Включите двухфакторную аутентификацию (2FA)
2. Создайте App Password:
   - Перейдите в настройки аккаунта Google
   - Безопасность → Двухэтапная аутентификация → Пароли приложений
   - Создайте пароль для "Почта" и "Другое устройство"
   - Скопируйте 16-символьный пароль
3. В настройках поставщика укажите:
   - **IMAP Host:** `imap.gmail.com`
   - **IMAP Port:** `993`
   - **Email:** ваш Gmail адрес
   - **App Password:** созданный пароль приложения
   - **Secure (TLS):** Да

#### Yandex

1. В настройках поставщика укажите:
   - **IMAP Host:** `imap.yandex.ru`
   - **IMAP Port:** `993`
   - **Email:** ваш Yandex адрес
   - **Password:** ваш пароль от почты
   - **Secure (TLS):** Да

#### Mail.ru

1. Включите двухфакторную аутентификацию (2FA)
2. Создайте пароль приложения
3. В настройках поставщика укажите:
   - **IMAP Host:** `imap.mail.ru`
   - **IMAP Port:** `993`
   - **Email:** ваш Mail.ru адрес
   - **App Password:** пароль приложения
   - **Secure (TLS):** Да

#### Outlook

1. В настройках поставщика укажите:
   - **IMAP Host:** `outlook.office365.com`
   - **IMAP Port:** `993`
   - **Email:** ваш Outlook адрес
   - **Password:** ваш пароль от почты
   - **Secure (TLS):** Да

### Проверка Email

**Ручная проверка:**
1. Нажмите кнопку "Check Email" (если доступна) или используйте кнопку 🔄 для парсинга
2. Система автоматически:
   - Подключится к почтовому серверу
   - Найдет письма по фильтрам
   - Извлечет Excel-вложения
   - Валидирует вложения
   - Сохранит валидные вложения
   - Запустит парсинг

**Автоматическая проверка:**
- При парсинге, если файлы отсутствуют на диске, система автоматически запускает "Check Email"
- Получает новые письма и сохраняет вложения
- Продолжает парсинг с новыми файлами

### Фильтры поиска

**Отправитель (From Email):**
- Укажите email адрес отправителя для фильтрации писем
- Оставьте пустым, чтобы искать письма от всех отправителей

**Фильтр по теме письма:**
- Укажите текст для поиска в теме письма
- Оставьте пустым, чтобы искать письма с любой темой

**Период поиска:**
- Количество дней для поиска писем назад (по умолчанию 90)
- Система ищет письма с указанной даты до сегодня

**Проверять в том числе прочитанные письма:**
- **Включено** — ищутся все письма (включая прочитанные)
- **Выключено** — ищутся только непрочитанные письма

**Использовать любое последнее вложение:**
- **Включено** — используется последнее вложение (включая обработанные)
- **Выключено** — используются только необработанные вложения

---

## Фильтры и поиск

### Поиск

**Поле поиска** в верхней части страницы тканей позволяет искать по:
- Названию коллекции
- Номеру/названию цвета
- Названию поставщика

**Особенности:**
- Регистронезависимый поиск
- Поиск по частичному совпадению
- Поиск работает в реальном времени

### Фильтры

#### Фильтр по поставщику

**Выпадающий список** с выбором поставщика:
- "Все поставщики" — показывает ткани от всех поставщиков
- Конкретный поставщик — показывает только ткани выбранного поставщика

#### Фильтр по наличию

**Чекбокс "Показывать только в наличии":**
- **Включено** — показывает только ткани с `inStock = true`
- **Выключено** — показывает все ткани

#### Фильтр по типу ткани

**Выпадающий список** с множественным выбором:
- Все уникальные типы тканей из базы данных
- Можно выбрать несколько типов одновременно
- "Все типы" — снимает фильтр

#### Фильтр по категории

**Выпадающий список** с выбором категории:
- Категории от 1 до 16
- "Все категории" — показывает ткани всех категорий

#### Фильтр по описанию

**Поле ввода** для поиска по тексту описания:
- Регистронезависимый поиск
- Поиск по частичному совпадению

### Сортировка

**Клик по заголовку колонки** позволяет сортировать данные:

- **Поставщик** — A-Z / Z-A
- **Коллекция** — A-Z / Z-A
- **Цвет** — A-Z / Z-A
- **Наличие** — true → false → null
- **Метраж** — по возрастанию / убыванию
- **Цена** — по возрастанию / убыванию
- **Категория** — 1 → 16

**Повторный клик** меняет направление сортировки.

---

## Редактирование данных

### Редактирование отдельной ткани

#### Цена

1. Кликните по цене в таблице
2. Введите новое значение
3. Нажмите "Сохранить" или клавишу Enter
4. Цена сохранится и останется до следующего парсинга

#### Тип ткани

1. Кликните по типу ткани в таблице
2. Введите или выберите тип
3. Нажмите "Сохранить" или клавишу Enter
4. Тип сохранится для этой ткани

#### Описание

1. Кликните по описанию в таблице (раскрывается по клику)
2. Введите описание
3. Нажмите "Сохранить" или клавишу Enter
4. Описание сохранится для этой ткани

### Редактирование коллекции

**Клик по названию коллекции** (в заголовке или в любой строке) открывает диалоговое окно с полями:

- **Тип ткани** — тип ткани для всей коллекции
- **Категория ткани** — категория для всей коллекции
- **Цена ткани** — цена для всей коллекции
- **Описание ткани** — описание для всей коллекции

**После сохранения:**
- Появляется диалог: "Применить описание ко всем цветам данной коллекции?"
- **Да** — все введенные данные применяются ко всем цветам в коллекции
- **Нет** — изменяется только описание коллекции

### Загрузка изображений

1. Нажмите кнопку "Добавить изображение" в строке ткани
2. Выберите файл изображения
3. Изображение загрузится и отобразится в таблице

---

## Категории тканей

### Система категоризации

Ткани автоматически распределяются по **16 категориям** на основе цены:

| Категория | Максимальная цена |
|-----------|-------------------|
| 1 | До 550₽ |
| 2 | До 600₽ |
| 3 | До 650₽ |
| 4 | До 700₽ |
| 5 | До 750₽ |
| 6 | До 800₽ |
| 7 | До 850₽ |
| 8 | До 900₽ |
| 9 | До 1000₽ |
| 10 | До 1200₽ |
| 11 | До 1500₽ |
| 12 | До 2000₽ |
| 13 | До 2500₽ |
| 14 | До 3000₽ |
| 15 | До 5000₽ |
| 16 | Свыше 5000₽ |

### Логика категоризации

- Если цена <= стоимости категории → эта категория
- Если цена больше → следующая категория

**Примеры:**
- 350₽ → категория 1
- 700₽ → категория 4
- 1200₽ → категория 10
- 7000₽ → категория 15
- 8500₽ → категория 16

### Управление категориями

**Страница:** `/categories`

**Функционал:**
- Просмотр всех категорий
- Редактирование цен категорий
- Автоматическое пересчитывание категорий при изменении цен

**Редактирование:**
1. Перейдите на страницу "Категории"
2. Кликните по цене категории
3. Введите новое значение
4. Нажмите "Сохранить"
5. Категории всех тканей пересчитаются автоматически

---

## Исключения из парсинга

### Принцип работы

Ткани можно **исключить из автоматического парсинга**. При следующем парсинге данные для исключенных тканей не будут обновляться.

**Использование:**
- Сохранение ручных изменений
- Исключение устаревших тканей
- Исключение тестовых данных

### Управление исключениями

**Страница:** `/exclusions`

**Интерфейс:**
- Группировка по поставщикам и коллекциям
- Чекбоксы для каждой ткани
- Визуальное отображение исключенных тканей (отмечены чекбоксом)

**Использование:**
1. Перейдите на страницу "Исключения"
2. Найдите нужную ткань (группировка по поставщикам и коллекциям)
3. Установите/снимите чекбокс для исключения/включения
4. Нажмите "Сохранить изменения"
5. Изменения применятся ко всем отмеченным тканям

**Восстановление:**
- Снимите чекбокс и нажмите "Сохранить изменения"
- Ткань снова будет участвовать в автоматическом парсинге

---

## Ручная загрузка данных

### Типы загрузок

#### 1. Загрузка наличия (Stock)

**Назначение:**
- Обновление метража и наличия тканей
- Приоритет над парсером до получения более новых данных

**Формат файла:**
- Excel (.xls или .xlsx)
- Должен содержать колонки: коллекция, цвет, метраж/наличие

**Использование:**
1. На странице "Поставщики" нажмите кнопку 📊 рядом с поставщиком
2. Выберите файл Excel с наличием
3. Нажмите "Загрузить"
4. Данные обновятся в базе

#### 2. Загрузка прайс-листа (Price)

**Назначение:**
- Установка цен на ткани
- Автоматическое определение типа прайс-листа

**Типы прайс-листов:**
- **Цена на цвет** — каждая строка = цвет с ценой
- **Цена на коллекцию** — одна цена для всех цветов коллекции

**Формат файла:**
- Excel (.xls или .xlsx)
- Должен содержать колонки: коллекция, цвет (опционально), цена

**Использование:**
1. На странице "Поставщики" нажмите кнопку 💰 рядом с поставщиком
2. Выберите файл Excel с прайс-листом
3. Нажмите "Загрузить"
4. Система автоматически определит тип прайс-листа и установит цены

### Приоритет данных

**Логика приоритета:**
1. Ручная загрузка наличия имеет приоритет до тех пор, пока парсер не получит более новые данные
2. Ручная загрузка прайс-листа используется для установки цен
3. Парсер обновляет данные только если они новее ручной загрузки

---

## Экспорт данных

### Экспорт в Excel

**Функционал:**
- Экспорт всех тканей или с применением фильтров
- Сохранение структуры данных
- Поддержка фильтров по поставщику и наличию

**Использование:**
1. На странице "Ткани" примените нужные фильтры (если требуется)
2. Нажмите кнопку "Экспорт в Excel" (иконка Download)
3. Файл будет скачан автоматически

**Формат файла:**
- Excel (.xlsx)
- Колонки: Поставщик, Коллекция, Цвет, Наличие, Метраж, Цена, Категория, Тип, Описание, Дата обновления, Дата поступления, Комментарий

---

## Правила парсинга для каждого поставщика

### Аметист

**Метод парсинга:** Email (Excel вложения, возможно ZIP)

**Структура данных:**
- Колонка C (индекс 2) — коллекция
- Колонка E (индекс 4) — цвет
- Колонка G (индекс 6) — метраж/наличие
- Колонка J (индекс 9) — дата следующего прихода

**Особенности:**
- Файлы могут приходить в формате ZIP, система автоматически распаковывает Excel файл
- Используется точное чтение значений с поддержкой запятых как десятичных разделителей
- Единица измерения "м" может быть в следующей колонке (H) или в той же ячейке
- Комментарий "ВНИМАНИЕ, МАЛО!" для метража меньше 10м
- Удаление первого слова цвета, если оно совпадает с коллекцией

**Логика метража:**
- Если число >= 10: `в наличии = true`, метраж = число
- Если число < 10: `в наличии = true`, метраж = число, комментарий = "ВНИМАНИЕ, МАЛО!"
- Если пусто или "нет": `в наличии = false`

### Нортекс

**Метод парсинга:** Email (Excel вложения)

**Структура данных:**
- Колонка C (индекс 2) — коллекция и цвет ("Ткань Collection Color")
- Столбец E (индекс 4) — галочка "V" для наличия < 100м
- Столбец F (индекс 5) — может быть галочка "V", метраж (цифры), дата прихода (текст), комментарий
- Столбец G (индекс 6) — если заполнен, в наличии, метраж 100+м

**Особенности:**
- Удаление префикса "Ткань" при парсинге
- Пропуск строк с "пог. м", "Ед.изм.", "отчет создан"
- Пропуск первых 10 строк (заголовки)
- Используется точное чтение значений с поддержкой запятых
- Комментарий "ВНИМАНИЕ, МАЛО!" для метража меньше 10м

**Логика обработки столбцов E, F, G:**
- **Столбец G заполнен** → в наличии, метраж = 100м
- **Столбец E = "V"** → в наличии, метраж < 100м (точный не указан)
- **Столбец F = "V"** → в наличии, метраж < 100м
- **Столбец F = цифры** → в наличии, метраж = значение
- **Столбец F = дата прихода** → нет в наличии, дата = значение
- **Столбец F = текст** → комментарий = значение
- **Все пусто** → нет в наличии

### NoFrames

**Метод парсинга:** Excel по URL

**Структура данных:**
- Колонка B (индекс 1) — коллекция и цвет
- Колонка D (индекс 3) — наличие (метраж)
- Колонка E (индекс 4) — дата следующего поступления

**Особенности:**
- Парсятся только вкладки "ОСНОВНЫЕ КОЛЛЕКЦИИ" и "РАСПРОДАЖА"
- Пропуск первых 6 строк (заголовки)
- Игнорирование строк только с названием коллекции (без цвета)
- Удаление префикса "ткань мебельная" и кавычек

**Логика наличия:**
- Если значение > 0: `в наличии = true`, метраж = значение
- Если значение = 0 или пусто: `в наличии = false`

### Vektor

**Метод парсинга:** Excel по URL (динамический)

**Структура данных:**
- Колонка A (индекс 0) — коллекция и цвет
- Колонка F (индекс 5) — метраж с наличием

**Особенности:**
- URL динамический: `https://api.vektor.club/static/remainders_files/{DDMMYY}_MSK.xlsx`
- Автоматический поиск файла начиная с сегодняшней даты по убыванию
- Только строки со словом "ткань" (игнорируется при парсинге)
- Игнорирование строк со словом "Сетка" (любой регистр)
- Пропуск первых 9 строк (заголовки)
- Удаление префикса "Ткань" и "КОЖ ЗАМ"
- "по запросу" = не в наличии, записывается в комментарий

**Логика наличия:**
- Если значение > 0: `в наличии = true`, метраж = значение
- Если значение = 0 или пусто: `в наличии = false`
- Если "по запросу": `в наличии = false`, комментарий = "по запросу"

### Эгида

**Метод парсинга:** Excel по URL (динамический)

**Структура данных:**
- Колонка B (индекс 1) — коллекция и цвет
- Колонка C (индекс 2) — наличие
- Колонка D (индекс 3) — комментарий
- Колонка E (индекс 4) — дата следующего поступления

**Особенности:**
- URL динамический: `https://exch.tendence.ru/download.php?file={DD.MM.YY}_ostatki_tkani_ooo_egida.xls`
- Автоматический поиск файла по датам
- Коллекция — текст до первой цифры
- Цвет — цифры + текст после

**Логика наличия:**
- "Есть в наличии" → `в наличии = true`
- "Мало" → `в наличии = true`, комментарий = "ВНИМАНИЕ, МАЛО!"
- "Нет в наличии" → `в наличии = false`

### Artvision

**Метод парсинга:** HTML

**Структура данных:**
- Первая колонка — коллекция+цвет (разделение по тире)
- Вторая колонка — наличие
- Третья колонка — метраж
- Четвертая колонка — дата

**Особенности:**
- Парсинг HTML таблиц
- Разделение коллекции и цвета по тире

### Viptextil

**Метод парсинга:** HTML

**Структура данных:**
- Первая колонка — коллекция и цвет
- Вторая колонка — наличие

**Особенности:**
- Парсинг HTML страниц
- Первое слово — коллекция, остальное — цвет (может быть текстом)
- Пропуск заголовков разделов

### TextileData

**Метод парсинга:** HTML

**Структура данных:**
- Товар — коллекция+цвет
- Остаток — метраж
- Макс.ролик — комментарий
- Дата прихода — дата

**Особенности:**
- Парсинг HTML таблиц
- Удаление "_\" и оставление только цифр для цвета

### TextileNova

**Метод парсинга:** HTML

**Структура данных:**
- Колонка A — коллекция и цвет
- Колонка B — остатки (метраж)
- Колонка C — дата

**Особенности:**
- Парсинг HTML страниц

### Союз-М

**Метод парсинга:** Excel по URL

**Структура данных:**
- Колонка B (индекс 1) — коллекция+цвет
- Колонка C (индекс 2) — наличие
- Колонка D (индекс 3) — дата
- Колонка E (индекс 4) — комментарий

### Домиарт

**Метод парсинга:** Excel по URL

**Структура данных:**
- Колонка A (индекс 0) — коллекция+цвет
- Колонка B (индекс 1) — наличие
- Колонка C (индекс 2) — дата

**Особенности:**
- Специальное правило для Alfa 2303

### Артекс

**Метод парсинга:** Excel по URL

**Структура данных:**
- Колонка A (индекс 0) — коллекция+цвет
- Колонка B (индекс 1) — наличие
- Колонка C (индекс 2) — дата

**Особенности:**
- Удаление префикса "Мебельная ткань"

### Tex.Group

**Метод парсинга:** Excel по URL

**Структура данных:**
- Колонка B (индекс 1) — коллекция и цвет
- Колонка D (индекс 3) — наличие

**Особенности:**
- Первое слово — коллекция, цифры и остальное — цвет

### Artefact

**Метод парсинга:** Excel по URL

**Особенности:**
- Парсинг Excel файлов по прямой ссылке

---

## Решение проблем

### Проблемы с парсингом

#### Ошибка: "No email attachments found"

**Причины:**
- Нет писем в почтовом ящике
- Фильтры слишком строгие
- Все письма уже обработаны

**Решения:**
1. Проверьте настройки email (отправитель, тема, период поиска)
2. Включите "Проверять в том числе прочитанные письма"
3. Включите "Использовать любое последнее вложение"
4. Запустите "Check Email" вручную

#### Ошибка: "Files are missing on disk"

**Причины:**
- Файлы были удалены с диска
- Файлы не были сохранены при получении email

**Решения:**
1. Система автоматически запускает "Check Email" при этой ошибке
2. Если автоматическая проверка не помогла, запустите "Check Email" вручную
3. Проверьте настройки email и убедитесь, что письма приходят

#### Ошибка: "Failed to execute 'json' on 'Response'"

**Причины:**
- Сервер вернул пустой или невалидный JSON ответ

**Решения:**
1. Обновлено: система теперь безопасно обрабатывает пустые ответы
2. Проверьте логи сервера для детальной информации
3. Попробуйте повторить операцию

#### Парсер не находит ткани

**Причины:**
- Неправильные правила парсинга
- Изменилась структура данных поставщика
- Файл не содержит данных

**Решения:**
1. Проведите анализ структуры данных заново
2. Проверьте правила парсинга в настройках поставщика
3. Убедитесь, что файл содержит данные

### Проблемы с Email

#### Не удается подключиться к почтовому серверу

**Решения:**
1. Проверьте правильность настроек (host, port, user, password)
2. Для Gmail/Mail.ru используйте App Password (не обычный пароль)
3. Убедитесь, что включен TLS (Secure = true)
4. Проверьте, что IMAP включен в настройках почтового ящика

#### Не находятся письма

**Решения:**
1. Проверьте фильтры (отправитель, тема)
2. Увеличьте период поиска
3. Включите "Проверять в том числе прочитанные письма"
4. Проверьте, что письма действительно есть в почтовом ящике

### Проблемы с данными

#### Данные не обновляются

**Решения:**
1. Проверьте, не исключена ли ткань из парсинга (страница "Исключения")
2. Проверьте статус поставщика (должен быть "active")
3. Запустите парсинг вручную
4. Проверьте логи сервера на наличие ошибок

#### Неправильные цены или категории

**Решения:**
1. Отредактируйте цену вручную на странице "Ткани"
2. Категория пересчитается автоматически
3. Или отредактируйте категорию напрямую через редактирование коллекции

---

## Заключение

Lavsit Textile предоставляет мощный инструментарий для автоматического парсинга и управления данными о тканях. Система поддерживает 14 поставщиков с различными методами парсинга, предоставляет гибкие фильтры и поиск, позволяет редактировать данные вручную и исключать ткани из автоматического парсинга.

Для получения технической информации см. [Полную документацию](./COMPLETE_DOCUMENTATION.md).

---

**Версия документа:** 2.0  
**Последнее обновление:** 2025-01-28




