# 📋 План автоматической настройки Secrets для Lavsit Textile

## 🎯 Цель

Автоматизировать настройку GitHub Secrets и Vercel Environment Variables без ручного ввода, используя переменные, которые уже есть в проекте или Supabase.

## 📊 Текущая ситуация

### Переменные, которые уже есть в проекте:

Из файла `scripts/fix-database-url.ts`:
- ✅ **Supabase Project ID**: `hduadapicktrcrqjvzvd`
- ✅ **Supabase Password**: `edcwsx123QAZ!`
- ✅ Можно построить `DATABASE_URL` автоматически

Из файла `AUTOMATED_DEPLOY.md`:
- ✅ **VERCEL_TOKEN**: `R7r2N1maVjii1BkkRQvidtls`
- ✅ **VERCEL_ORG_ID**: `team_2FyqWSswogxney3SWR8bxRzV`
- ✅ **VERCEL_PROJECT_ID**: `prj_bMA2mQ3UsVKhrjJsHqSiZ1rdj15K`

### Переменные, которые нужно получить из Supabase:

1. **DATABASE_URL** - можно построить из известных данных:
   ```
   postgresql://postgres.hduadapicktrcrqjvzvd:edcwsx123QAZ!@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require
   ```

2. **NEXT_PUBLIC_SUPABASE_URL** (если используется клиент):
   ```
   https://hduadapicktrcrqjvzvd.supabase.co
   ```

3. **NEXT_PUBLIC_SUPABASE_ANON_KEY** (если используется клиент):
   - Требуется получить из Supabase Dashboard

## 🚀 План автоматизации

### Шаг 1: Создать скрипт для автоматической настройки

✅ **Выполнено**: Создан скрипт `scripts/auto-setup-secrets.ps1`

### Шаг 2: Автоматическое извлечение переменных

Скрипт будет:

1. **Читать из .env.local** (если существует):
   - DATABASE_URL
   - EMAIL_CHECKER_API_KEY (опционально)

2. **Строить DATABASE_URL автоматически** из известных данных:
   - Используя Supabase Project ID и Password
   - Генерируя Connection Pooler URL

3. **Использовать известные Vercel credentials**:
   - Из файла AUTOMATED_DEPLOY.md или .vercel-token

### Шаг 3: Настройка GitHub Secrets

Скрипт автоматически установит через GitHub CLI:

**Обязательные:**
- `DATABASE_URL` - из .env.local или построенный автоматически
- `VERCEL_TOKEN` - из известных данных
- `VERCEL_ORG_ID` - из известных данных  
- `VERCEL_PROJECT_ID` - из известных данных

**Опциональные:**
- `EMAIL_CHECKER_API_KEY` - если указан в .env.local

### Шаг 4: Настройка Vercel Environment Variables

Скрипт автоматически установит через Vercel CLI:

**Обязательные:**
- `DATABASE_URL` - для production, preview, development

**Опциональные:**
- `EMAIL_CHECKER_API_KEY` - если указан

## 📝 Использование

### Вариант 1: Полная автоматизация (рекомендуется)

```powershell
# 1. Убедитесь, что .env.local существует с DATABASE_URL
#    Если нет - скрипт построит его автоматически

# 2. Запустите скрипт
npm run secrets:setup

# 3. Для недостающих переменных (VERCEL_*) скрипт покажет инструкции
```

### Вариант 2: Только GitHub Secrets

```powershell
npm run secrets:github-only
```

### Вариант 3: Только Vercel Environment Variables

```powershell
npm run secrets:vercel-only
```

### Вариант 4: Dry Run (проверка без изменений)

```powershell
npm run secrets:setup-dry
```

## ⚙️ Требования

### Установленные инструменты:

1. **GitHub CLI** (gh):
   ```powershell
   winget install GitHub.cli
   # или скачать с https://cli.github.com
   ```

2. **Vercel CLI**:
   ```powershell
   npm install -g vercel
   ```

3. **Авторизация:**
   ```powershell
   # GitHub
   gh auth login
   
   # Vercel
   vercel login
   ```

## 🔍 Что делает скрипт

1. ✅ Читает переменные из `.env.local` (или `.env`)
2. ✅ Проверяет наличие GitHub CLI и авторизацию
3. ✅ Проверяет наличие Vercel CLI и авторизацию
4. ✅ Автоматически устанавливает GitHub Secrets
5. ✅ Автоматически устанавливает Vercel Environment Variables
6. ✅ Показывает отчет о недостающих переменных
7. ✅ Предоставляет инструкции для ручной настройки недостающих

## 📋 Переменные, которые нужно ввести вручную (если отсутствуют)

Если скрипт обнаружит недостающие переменные, он покажет инструкции. Основные случаи:

### 1. DATABASE_URL не найден

Если в `.env.local` нет `DATABASE_URL`, скрипт может:
- Попробовать построить из известных данных Supabase
- Или попросить ввести вручную

**Решение:** Добавьте в `.env.local`:
```env
DATABASE_URL="postgresql://postgres.hduadapicktrcrqjvzvd:edcwsx123QAZ!@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require"
```

### 2. VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID

Эти данные уже известны из проекта:
- VERCEL_TOKEN: `R7r2N1maVjii1BkkRQvidtls`
- VERCEL_ORG_ID: `team_2FyqWSswogxney3SWR8bxRzV`
- VERCEL_PROJECT_ID: `prj_bMA2mQ3UsVKhrjJsHqSiZ1rdj15K`

**Решение:** Скрипт может использовать эти значения автоматически, или их можно добавить в `.env.local`:
```env
VERCEL_TOKEN="R7r2N1maVjii1BkkRQvidtls"
VERCEL_ORG_ID="team_2FyqWSswogxney3SWR8bxRzV"
VERCEL_PROJECT_ID="prj_bMA2mQ3UsVKhrjJsHqSiZ1rdj15K"
```

### 3. EMAIL_CHECKER_API_KEY (опционально)

Если нужна защита API endpoint проверки email:
```env
EMAIL_CHECKER_API_KEY="your-secret-key-here"
```

## 🎯 Быстрый старт

1. **Создайте файл `.env.local`** (если еще нет):
   ```powershell
   # Минимальная конфигурация
   DATABASE_URL="postgresql://postgres.hduadapicktrcrqjvzvd:edcwsx123QAZ!@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require"
   
   # Опционально
   EMAIL_CHECKER_API_KEY="your-secret-key"
   ```

2. **Установите и авторизуйтесь в CLI**:
   ```powershell
   # GitHub CLI
   winget install GitHub.cli
   gh auth login
   
   # Vercel CLI
   npm install -g vercel
   vercel login
   ```

3. **Запустите автоматическую настройку**:
   ```powershell
   npm run secrets:setup
   ```

4. **Если что-то отсутствует** - скрипт покажет инструкции для ручной настройки

## ✅ Чеклист

- [ ] GitHub CLI установлен и авторизован
- [ ] Vercel CLI установлен и авторизован
- [ ] Файл `.env.local` создан с `DATABASE_URL`
- [ ] Запущен `npm run secrets:setup`
- [ ] Все GitHub Secrets настроены
- [ ] Все Vercel Environment Variables настроены
- [ ] Проверена работа деплоя

## 🔧 Улучшения для будущего

1. **Автоматическое получение Supabase credentials через API**
2. **Автоматическое создание Vercel Postgres базы данных**
3. **Генерация EMAIL_CHECKER_API_KEY автоматически**
4. **Валидация всех переменных перед настройкой**

---

**Готово!** Теперь можно запустить `npm run secrets:setup` для автоматической настройки! 🚀


