# 🔧 Инструкция по применению миграций к базе данных Supabase

## ⚠️ Автоматическое применение невозможно

База данных недоступна для прямого подключения с локальной машины или с Vercel. Это может быть из-за:
- Режима паузы Free tier
- Network restrictions (IP whitelist)
- Проблем с DNS

## ✅ Решение: Ручное применение через Supabase SQL Editor

### Шаг 1: Откройте Supabase SQL Editor

1. Перейдите на: https://supabase.com/dashboard/project/hduadapicktrcrqjvzvd
2. В левом сайдбаре найдите **"SQL Editor"** и нажмите на него
3. Нажмите кнопку **"New Query"** (или используйте уже открытый редактор)

### Шаг 2: Скопируйте SQL миграцию

1. Откройте файл: `prisma/migrations/init_postgresql/migration.sql`
2. Или откройте готовый файл: `apply-migrations-now.sql` (уже создан)
3. Скопируйте **весь** SQL скрипт (143 строки)

### Шаг 3: Вставьте и выполните SQL

1. Вставьте скопированный SQL в SQL Editor
2. Проверьте, что SQL правильный (должен начинаться с `CREATE TABLE IF NOT EXISTS`)
3. Нажмите кнопку **"Run"** (или используйте `Ctrl+Enter` / `Cmd+Enter`)
4. Дождитесь выполнения

### Шаг 4: Проверка результата

После выполнения вы должны увидеть:
- ✅ **"Success. No rows returned"** - это нормально для CREATE TABLE команд
- Или список выполненных команд без ошибок

#### Проверка таблиц:

Выполните следующий SQL в SQL Editor для проверки созданных таблиц:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
AND table_type = 'BASE TABLE'
ORDER BY table_name;
```

Должны быть видны следующие таблицы:
- ✅ Supplier
- ✅ Fabric
- ✅ ParsingRule
- ✅ DataStructure
- ✅ EmailAttachment
- ✅ FabricCategory
- ✅ ManualUpload

### Шаг 5: Проверка подключения

После успешного применения миграций проверьте подключение:

1. Откройте: https://lavsit-textile.vercel.app/api/test-db
2. Должен вернуться JSON с `success: true` и списком таблиц

## 📋 Альтернативные методы

### Метод 2: Использование Supabase CLI

Если у вас есть доступ к Supabase CLI с правильным токеном:

```bash
# Установка Supabase CLI
npm install -g supabase

# Авторизация (нужен Management API token, не service role key)
supabase login

# Связывание с проектом
supabase link --project-ref hduadapicktrcrqjvzvd

# Применение миграций
supabase db push
```

**Примечание**: Service Role Key (`sb_secret_...`) не подходит для CLI. Нужен Management API token формата `sbp_...`.

### Метод 3: Через Vercel API Endpoint

Создан специальный endpoint для применения миграций: `/api/apply-migrations`

Но он требует, чтобы база данных была доступна с Vercel. Если база недоступна, этот метод не сработает.

## 🔍 Диагностика проблем

### Проблема: "Can't reach database server"

**Возможные причины:**
1. База данных в режиме паузы (Free tier)
2. Network restrictions (IP whitelist)
3. Неправильный DATABASE_URL

**Решение:**
- Проверьте статус базы данных в Supabase Dashboard
- Убедитесь, что база данных не в режиме паузы
- Проверьте настройки Network Access
- Используйте ручное применение через SQL Editor

### Проблема: "Invalid access token format" при использовании CLI

**Причина:** Service Role Key не подходит для CLI

**Решение:** 
- Используйте Management API token (`sbp_...`)
- Или используйте ручное применение через SQL Editor

### Проблема: "ENOTFOUND" или DNS ошибки

**Причина:** База данных недоступна с вашей сети

**Решение:**
- Используйте ручное применение через SQL Editor
- Или проверьте настройки Network Access в Supabase

## ✅ После успешного применения миграций

1. ✅ Все таблицы созданы
2. ✅ Можно протестировать подключение: https://lavsit-textile.vercel.app/api/test-db
3. ✅ Можно использовать все функции приложения
4. ✅ Можно тестировать парсинг для каждого поставщика

## 📝 SQL файл для копирования

Файл с готовым SQL: `apply-migrations-now.sql` (уже создан в корне проекта)

Этот файл содержит весь SQL скрипт для применения миграций. Просто скопируйте его содержимое и вставьте в Supabase SQL Editor.

---

**Дата создания инструкции**: После попыток автоматического применения миграций  
**Статус**: Требуется ручное применение через SQL Editor

