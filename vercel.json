{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 300
    }
  },
  "buildCommand": "npx puppeteer browsers install chrome && prisma generate && next build",
  "installCommand": "npm install",
  "crons": []
}

