# 🤖 Полная автоматизация lavsit-textile

## ✅ Настроено и работает

### 1. Автоматическое сохранение на GitHub

**Компоненты:**
- ✅ Git post-commit hook - автоматически отправляет изменения на GitHub после каждого коммита
- ✅ Скрипт автоматического коммита (`auto-commit-and-push.ts`) - отслеживает изменения файлов и автоматически коммитит их
- ✅ PowerShell скрипт для фоновой службы (`start-auto-commit-service.ps1`) - запускает авто-коммит в фоне

**Как работает:**
1. При любом коммите (ручном или автоматическом) срабатывает `post-commit` hook
2. Hook автоматически выполняет `git push origin <branch>` на GitHub
3. При использовании `npm run auto:watch` или `npm run auto:start` все изменения файлов отслеживаются и автоматически коммитятся каждые 5 секунд после последнего изменения

### 2. Автоматический деплой в Vercel

**Компоненты:**
- ✅ GitHub Actions workflow (`.github/workflows/auto-deploy.yml`)
- ✅ Автоматически запускается при каждом push в ветку `main`

**Как работает:**
1. При push на GitHub автоматически запускается GitHub Actions
2. Workflow устанавливает зависимости, генерирует Prisma Client
3. Деплоит проект в Vercel используя Vercel CLI
4. Использует secrets из GitHub: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`

**Настройка secrets (требуется один раз):**
1. Перейдите: https://github.com/edcwsx23QAZ/Lavsit-Textile/settings/secrets/actions
2. Добавьте secrets:
   - `VERCEL_TOKEN`: ваш Vercel API токен
   - `VERCEL_ORG_ID`: ID организации Vercel
   - `VERCEL_PROJECT_ID`: ID проекта Vercel

### 3. Локальное хранение 50 последних ревизий

**Компоненты:**
- ✅ Скрипт управления ревизиями (`manage-revisions.ts`)
- ✅ Директория `.revisions/` для хранения git bundles
- ✅ Автоматическая очистка старых ревизий (оставляет только 50 последних)

**Как работает:**
1. При каждом коммите автоматически сохраняется git bundle в `.revisions/`
2. Сохраняются метаданные (hash, дата, сообщение коммита)
3. При сохранении новой ревизии автоматически удаляются старые (остается только 50 последних)
4. Ревизии сохраняются как git bundles и могут быть восстановлены

## 🚀 Быстрый старт

### Первоначальная настройка

```powershell
# Перейдите в директорию проекта
cd "E:\Work programs\cursor\repositary\lavsit-textile"

# Запустите скрипт полной настройки
.\scripts\setup-complete-automation.ps1
```

Этот скрипт:
- ✅ Установит все зависимости
- ✅ Настроит Git hooks
- ✅ Создаст директорию для ревизий
- ✅ Сохранит текущую ревизию
- ✅ Проверит настройки GitHub Actions

### Запуск автоматического коммита

**Вариант 1: Режим отслеживания в консоли**
```bash
npm run auto:watch
```
Работает пока консоль открыта, показывает все изменения в реальном времени.

**Вариант 2: Фоновая служба (автономный режим)**
```bash
npm run auto:start
```
Запускает процесс в фоне, работает независимо от IDE и консоли.

**Проверка статуса:**
```bash
npm run auto:status
```

**Остановка:**
```bash
npm run auto:stop
```

### Ручной коммит и push

```bash
npm run auto:commit
```

## 📋 Все доступные команды

### Автоматизация коммита

| Команда | Описание |
|---------|----------|
| `npm run auto:watch` | Запуск режима отслеживания изменений (в консоли) |
| `npm run auto:start` | Запуск авто-коммита как фоновой службы |
| `npm run auto:stop` | Остановка фоновой службы |
| `npm run auto:status` | Проверка статуса фоновой службы |
| `npm run auto:commit` | Ручной коммит и push всех изменений |

### Управление ревизиями

| Команда | Описание |
|---------|----------|
| `npm run revisions:save` | Сохранить текущую ревизию |
| `npm run revisions:list` | Показать список сохраненных ревизий |
| `npm run revisions:cleanup` | Очистить старые ревизии (оставить только 50) |
| `npm run revisions:restore <hash>` | Восстановить ревизию по hash |

## 🔧 Настройки

### Изменение задержки перед коммитом

В `scripts/auto-commit-and-push.ts`:
```typescript
const COMMIT_DELAY = 5000 // 5 секунд (в миллисекундах)
```

### Изменение количества сохраняемых ревизий

В `scripts/manage-revisions.ts`:
```typescript
const MAX_REVISIONS = 50 // Количество сохраняемых ревизий
```

### Игнорируемые файлы

Система автоматически игнорирует:
- `node_modules/`
- `.next/`
- `.git/`
- `.vercel/`
- `.env*`
- `.revisions/`
- Логи и временные файлы

Настройка: `IGNORE_PATTERNS` в `scripts/auto-commit-and-push.ts`

## 📊 Мониторинг

### Проверка статуса GitHub Actions

1. Откройте: https://github.com/edcwsx23QAZ/Lavsit-Textile/actions
2. Проверьте последние workflow runs
3. Откройте конкретный run для просмотра логов

### Проверка статуса Vercel деплоя

1. Откройте: https://vercel.com/dashboard
2. Выберите проект `lavsit-textile`
3. Проверьте вкладку **Deployments**

### Проверка локальных ревизий

```bash
npm run revisions:list
```

### Просмотр логов авто-коммита

При использовании фоновой службы:
```powershell
Get-Content auto-commit.log -Tail 50 -Wait
```

## 🛡️ Безопасность

### Важно:

1. **Git hooks не выполняются автоматически при merge/rebase** - это предотвращает конфликты
2. **Автоматический коммит проверяет изменения** - не коммитит пустые изменения
3. **Игнорируются системные файлы** - `.env`, `node_modules`, `.git` не коммитятся автоматически
4. **GitHub Actions требует secrets** - без правильных secrets деплой не запустится
5. **Ревизии хранятся локально** - `.revisions/` не отправляется на GitHub по умолчанию

### Рекомендации:

- Используйте `.gitignore` для исключения чувствительных файлов
- Регулярно проверяйте логи GitHub Actions
- Используйте `--dry-run` режим для проверки перед включением авто-коммита
- Храните Vercel токены только в GitHub Secrets, не коммитьте их в код

## 🔄 Автономная работа

Система работает полностью автономно:

1. **Git hooks** работают независимо от IDE - срабатывают при любом коммите
2. **Фоновая служба** (`npm run auto:start`) работает независимо от консоли и IDE
3. **GitHub Actions** работает на серверах GitHub, не требует локального подключения после push
4. **Vercel деплой** запускается автоматически через GitHub Actions

### Запуск при старте Windows

1. Откройте планировщик заданий (`taskschd.msc`)
2. Создайте новое задание
3. Триггер: **При входе пользователя**
4. Действие: **Запустить программу**
5. Программа: `powershell.exe`
6. Аргументы: `-ExecutionPolicy Bypass -File "E:\Work programs\cursor\repositary\lavsit-textile\scripts\start-auto-commit-service.ps1"`

## 🆘 Решение проблем

### Проблема: Git hook не выполняется

**Решение:**
```powershell
# Проверьте права на файл
icacls .git\hooks\post-commit

# Запустите скрипт настройки
.\scripts\setup-complete-automation.ps1
```

### Проблема: GitHub Actions не запускается

**Решение:**
1. Проверьте, что secrets добавлены: https://github.com/edcwsx23QAZ/Lavsit-Textile/settings/secrets/actions
2. Проверьте, что workflow файл существует: `.github/workflows/auto-deploy.yml`
3. Проверьте логи в разделе Actions

### Проблема: Автоматический коммит коммитит слишком много

**Решение:**
- Обновите `IGNORE_PATTERNS` в `scripts/auto-commit-and-push.ts`
- Используйте `.gitignore` для исключения ненужных файлов

### Проблема: Ревизии не сохраняются

**Решение:**
```bash
# Проверьте права на директорию .revisions
# Убедитесь, что есть место на диске
# Попробуйте сохранить вручную:
npm run revisions:save
```

### Проблема: Фоновая служба не запускается

**Решение:**
```powershell
# Проверьте статус
npm run auto:status

# Проверьте логи
Get-Content auto-commit.log

# Убедитесь, что Node.js установлен
node --version

# Переустановите зависимости
npm install
```

## ✅ Чеклист проверки

- [x] GitHub Actions workflow настроен
- [x] Git hooks настроены
- [x] Скрипты автоматического коммита работают
- [x] Система хранения 50 ревизий работает
- [x] Фоновая служба запускается
- [ ] GitHub Actions secrets добавлены (требуется ручное действие)
- [ ] Тестовый коммит выполнен и автоматически отправлен на GitHub
- [ ] GitHub Actions автоматически запустился после push
- [ ] Vercel автоматически задеплоил проект

## 🎯 Итог

После настройки:

1. ✅ Все изменения автоматически коммитятся и отправляются на GitHub
2. ✅ GitHub Actions автоматически деплоит в Vercel при каждом push
3. ✅ Локально сохраняются последние 50 ревизий
4. ✅ Можно восстановить любую из 50 последних ревизий
5. ✅ Система работает автономно, независимо от IDE

---

**Готово!** Автоматизация полностью настроена. Теперь все изменения будут автоматически сохраняться на GitHub и деплоиться в Vercel! 🚀


