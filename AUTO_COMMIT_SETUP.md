# 🤖 Настройка автоматического коммита, push и деплоя

## 📋 Обзор

Данная система автоматизации обеспечивает:
1. ✅ **Автоматическое сохранение на GitHub** - все изменения автоматически коммитятся и отправляются на GitHub
2. ✅ **Автоматический деплой в Vercel** - после каждого push на GitHub автоматически запускается деплой в Vercel
3. ✅ **Хранение 50 последних ревизий** - локально сохраняются последние 50 версий приложения

## 🚀 Быстрый старт

### Шаг 1: Настройка GitHub Actions secrets

1. Перейдите на: https://github.com/edcwsx23QAZ/Lavsit-Textile/settings/secrets/actions
2. Нажмите **"New repository secret"**
3. Добавьте следующие secrets:

   | Name | Value | Описание |
   |------|-------|----------|
   | `VERCEL_TOKEN` | `R7r2N1maVjii1BkkRQvidtls` | Vercel API токен |
   | `VERCEL_ORG_ID` | `team_2FyqWSswogxney3SWR8bxRzV` | ID организации Vercel |
   | `VERCEL_PROJECT_ID` | `prj_bMA2mQ3UsVKhrjJsHqSiZ1rdj15K` | ID проекта Vercel |

4. После добавления всех secrets, GitHub Actions автоматически запустится при следующем push

### Шаг 2: Настройка git hooks (автоматический push)

**Для Windows (PowerShell):**

```powershell
# Запустите скрипт настройки
.\scripts\setup-auto-commit.ps1
```

Этот скрипт:
- Настроит git hooks для автоматического push после коммита
- Создаст директорию `.revisions` для хранения ревизий
- Сохранит текущую ревизию

### Шаг 3: Запуск режима отслеживания изменений (опционально)

Для автоматического коммита всех изменений в реальном времени:

```bash
# TypeScript версия (кросс-платформенная)
npm run auto:watch

# Или PowerShell версия (только Windows)
.\scripts\auto-commit-and-push.ps1
```

**Как это работает:**
- Скрипт отслеживает все изменения в файлах проекта
- При обнаружении изменений ждет 10 секунд (настраивается)
- Автоматически создает коммит и отправляет на GitHub
- Сохраняет текущую ревизию локально
- Очищает старые ревизии (оставляет только последние 50)

## 📝 Доступные команды

### Автоматизация коммита и push

```bash
# Запуск режима отслеживания изменений (автоматический коммит)
npm run auto:watch

# Ручной коммит и push всех изменений
npm run auto:commit
```

### Управление ревизиями

```bash
# Сохранить текущую ревизию
npm run revisions:save

# Показать список сохраненных ревизий
npm run revisions:list

# Очистить старые ревизии (оставить только 50 последних)
npm run revisions:cleanup

# Восстановить ревизию по hash
npm run revisions:restore <hash>
```

## 🔧 Как это работает

### 1. Автоматический push через git hooks

**Файл:** `.git/hooks/post-commit` (или `.git/hooks/post-commit.ps1`)

После каждого коммита автоматически выполняется:
1. Проверка наличия изменений для отправки
2. Автоматический `git push origin <branch>`
3. Логирование результата

### 2. Автоматический деплой через GitHub Actions

**Файл:** `.github/workflows/auto-deploy.yml`

При каждом push в ветку `main`:
1. GitHub Actions автоматически запускается
2. Устанавливает зависимости
3. Генерирует Prisma Client
4. Деплоит проект в Vercel
5. Сохраняет результат деплоя

### 3. Хранение локальных ревизий

**Директория:** `.revisions/`

Система автоматически сохраняет:
- Git bundle для каждой ревизии
- Метаданные (hash, дата, сообщение коммита)
- Автоматически очищает старые ревизии (оставляет только 50 последних)

## ⚙️ Настройки

### Изменение задержки перед коммитом

В `scripts/auto-commit-and-push.ts` измените:

```typescript
const COMMIT_DELAY = 5000 // 5 секунд (в миллисекундах)
```

В `scripts/auto-commit-and-push.ps1`:

```powershell
param(
    [int]$CommitDelay = 10,  # Задержка в секундах
)
```

### Изменение количества сохраняемых ревизий

В `scripts/manage-revisions.ts` измените:

```typescript
const MAX_REVISIONS = 50 // Количество сохраняемых ревизий
```

### Игнорируемые файлы

Система автоматически игнорирует:
- `node_modules/`
- `.next/`
- `.git/`
- `.vercel/`
- `.env*`
- `.revisions/`
- Логи и временные файлы

## 📊 Структура файлов

```
lavsit-textile/
├── .github/
│   └── workflows/
│       └── auto-deploy.yml          # GitHub Actions для автоматического деплоя
├── .git/
│   └── hooks/
│       ├── post-commit              # Git hook для автоматического push
│       └── post-commit.ps1          # PowerShell версия hook
├── .revisions/                      # Локальные ревизии (git bundles)
│   ├── revision-abc1234-20250101-120000.bundle
│   └── revision-abc1234-20250101-120000.meta.json
└── scripts/
    ├── auto-commit-and-push.ts      # TypeScript версия авто-коммита
    ├── auto-commit-and-push.ps1     # PowerShell версия авто-коммита
    ├── manage-revisions.ts          # Управление ревизиями
    └── setup-auto-commit.ps1        # Скрипт настройки
```

## 🔍 Мониторинг

### Проверка статуса GitHub Actions

1. Откройте: https://github.com/edcwsx23QAZ/Lavsit-Textile/actions
2. Проверьте последние workflow runs
3. Откройте конкретный run для просмотра логов

### Проверка статуса Vercel деплоя

1. Откройте: https://vercel.com/dashboard
2. Выберите проект `lavsit-textile`
3. Проверьте вкладку **Deployments**

### Проверка локальных ревизий

```bash
npm run revisions:list
```

## 🛡️ Безопасность

### Важно:

1. **Git hooks не выполняются автоматически при merge/rebase** - это предотвращает конфликты
2. **Автоматический коммит проверяет изменения** - не коммитит пустые изменения
3. **Игнорируются системные файлы** - `.env`, `node_modules`, `.git` не коммитятся автоматически
4. **GitHub Actions требует secrets** - без правильных secrets деплой не запустится

### Рекомендации:

- Используйте `.gitignore` для исключения чувствительных файлов
- Регулярно проверяйте логи GitHub Actions
- Используйте `--dry-run` режим для проверки перед включением авто-коммита

## 🆘 Решение проблем

### Проблема: Git hook не выполняется

**Решение:**
```powershell
# Проверьте права на файл
icacls .git\hooks\post-commit

# Сделайте файл исполняемым
icacls .git\hooks\post-commit /grant Everyone:RX
```

### Проблема: GitHub Actions не запускается

**Решение:**
1. Проверьте, что secrets добавлены: https://github.com/edcwsx23QAZ/Lavsit-Textile/settings/secrets/actions
2. Проверьте, что workflow файл существует: `.github/workflows/auto-deploy.yml`
3. Проверьте логи в разделе Actions

### Проблема: Автоматический коммит коммитит слишком много

**Решение:**
- Обновите `IGNORE_PATTERNS` в `scripts/auto-commit-and-push.ts`
- Используйте `.gitignore` для исключения ненужных файлов
- Используйте `--dry-run` режим для проверки

### Проблема: Ревизии не сохраняются

**Решение:**
```bash
# Проверьте права на директорию .revisions
# Убедитесь, что есть место на диске
# Проверьте логи скрипта
```

## ✅ Чеклист настройки

- [ ] GitHub Actions secrets добавлены (VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID)
- [ ] Git hooks настроены (`npm run setup:auto-commit` или `.\scripts\setup-auto-commit.ps1`)
- [ ] Директория `.revisions` создана
- [ ] Текущая ревизия сохранена (`npm run revisions:save`)
- [ ] Тестовый коммит выполнен и автоматически отправлен на GitHub
- [ ] GitHub Actions автоматически запустился после push
- [ ] Vercel автоматически задеплоил проект после GitHub Actions
- [ ] Локальные ревизии сохраняются при коммитах

## 🎯 После настройки

После успешной настройки:

1. ✅ Все изменения автоматически коммитятся и отправляются на GitHub
2. ✅ GitHub Actions автоматически деплоит в Vercel при каждом push
3. ✅ Локально сохраняются последние 50 ревизий
4. ✅ Можно восстановить любую из 50 последних ревизий

---

**Готово!** Автоматизация настроена. Теперь все изменения будут автоматически сохраняться на GitHub и деплоиться в Vercel! 🚀


